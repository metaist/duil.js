define(function () {
  'use strict';

  /** Data + UI + Loop = duil  */
  var duil = {};

  /** Semantic Version <http://semver.org> */
  duil.VERSION = '@VERSION';

  return duil; // build ignore:line
});
