define([
  'jquery',
  'lodash',
  './duil.core'
], function ($, _, duil) {
  'use strict'; // build ignore:line

  duil.http = function (method, url, data, callback) {

  };

  duil.http.poll = function (url, data, callback) {

  };

  duil.http.websocket = function (url, data, callback) {

  };
});
